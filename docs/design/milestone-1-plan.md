# Milestone 1 implementation plan — sparse-image installer, image-only

Status: **Gate A fully passes (2026-09-22) — first time in this project's history.** Routed around the still-unresolved guestfwd blocker (decision record 16) by using the interactive Terminal UI installer (proven reliable for last night's real `/dev/sdc` migration) instead of the automated/answer-file path, then staging Fixture A's exact byte content directly onto the fresh install. The additive repair pipeline, run for real against genuine ifupdown2/dhclient/SLIRP, achieved a full apply-time pass: address, route, gateway, DNS, and HTTPS all independently re-verified afterward, not just trusted from the pipeline's own report. The reboot-persistence gap this same investigation found and confirmed directly (a clean `reboot` left `vmbr0` with no IPv4 address or route at all — `ifupdown2`'s boot dispatch only ever processes the first `iface vmbr0` stanza) was then closed: a new, narrowly-scoped boot-time systemd unit (`repair_additive_persist.py` + `baseline-additive-dhcp-reapply.service`, 6 new unit tests, 87/87 total passing) re-runs `dhclient` only for interfaces carrying the additive repair's own signature, idempotently. Independently re-verified in a second real QEMU run: after reboot, address, route, gateway, DNS, and HTTPS all pass again automatically, with zero manual intervention. Also found along the way: SLIRP's DHCP router-option behavior (decision record 15's finding) is **inconsistent across sessions** on this same host — one pass's SLIRP server sent a `routers` option where an earlier one did not, with no code change between them — not resolved to a single explanation, noted for future reference. See [decision-records/12-gateA-additive-repair.md](decision-records/12-gateA-additive-repair.md) through [17-gateA-apply-time-pass-reboot-gap-confirmed.md](decision-records/17-gateA-apply-time-pass-reboot-gap-confirmed.md). The separately-unresolved guestfwd infrastructure problem (decision record 16) still blocks the *automated/answer-file* install path specifically (relevant to building a fresh disposable image without manual TUI interaction) but no longer blocks Gate A verification itself. **No push to `cliffthelin/baseline` at any point. Gates B–F may now begin.**
**Gate B also fully passes (2026-09-22).** Boundary 1 (`drive_setup_acquire.py` — GPG-signed-`Release` -> hash-pinned-`Packages` -> hash-pinned-`.deb` chain, 11 unit tests) and boundary 2 (`drive_setup_answer.py` — workspace/credential/ephemeral-answer-server/`prepare_iso_defensively`, consolidated from the four proven `experiments/m0-inv3` modules, 17 unit tests) are both promoted to `baseline/lib/` and real-verified: a real `prepare-iso` run against the real assistant binary and a real ephemeral answer server produced a prepared ISO whose every postcondition — fetch mode, URL, cert fingerprint, absence of forbidden canaries (raw byte scan) — was independently confirmed via `inspect-iso`, exactly Gate B's stated evidence requirement. 115/115 unit tests passing. See [decision-records/18-boundary1-acquire-module-promoted.md](decision-records/18-boundary1-acquire-module-promoted.md) and [19-boundary2-answer-module-gateB-passes.md](decision-records/19-boundary2-answer-module-gateB-passes.md). One real gap flagged, not yet closed: `preflight.py`'s three constraints (HTTPS required, cert-fingerprint required, no auth-token) are test-only infrastructure per the PRD, not promoted — whoever writes the real orchestration layer calling `prepare_iso_defensively` must enforce them explicitly. Gate C (boundary 3, `drive_setup_install.py`) is next.

**Gate C fully passes (2026-09-23).** `baseline/lib/drive_setup_install.py` is written and unit-tested (14 new tests, 129/129 total): QEMU invocation construction (sparse-file-only, `guestfwd`-isolated networking, `order=d,once=d` for install / `order=c` with no CD-ROM attached for post-install boot per decision record 04's fix), bounded process waiting with process-group kill on timeout, static (`fdisk`/`blkid`-only, never mount/loop-device) image verification, and streamed forbidden-byte scanning. Decision record 20 first real-verified the module's own automatable mechanisms (real QEMU launch via `RealInstallRunner.popen`, real HMP monitor-socket `send_monitor_command`) but did not capture the literal completion text, blocked by transient VNC keyboard-layout non-determinism. A follow-up pass on a fresh QEMU boot closed that gap: `@` typed correctly this time, and with "automatically reboot after successful installation" explicitly unchecked, a tight screendump poll caught the installer's own literal completion dialog — `Success` / `Installation finished - reboot now?` — read directly from the screendump by this session, never inferred from QEMU exit code or partition structure. As a bonus, the installed disk was then booted with no installer media attached (`build_postinstall_boot_invocation`) and reached a working `baseline login:` prompt with the correct hostname and configured IP, giving preliminary Gate D evidence. See [decision-records/20-boundary3-install-module-partial.md](decision-records/20-boundary3-install-module-partial.md) and [21-gateC-passes-explicit-success-captured.md](decision-records/21-gateC-passes-explicit-success-captured.md). Gate D proper (fresh-OVMF boot reproduces the *same persisted broken* static configuration Phase 0 observed, a specific fixture-reproduction test) is next.

**Gate D passes (2026-09-23).** Switched from TUI keystroke-driving to the real headless answer-file path per direct instruction: `prepare-iso --fetch-from iso` embeds the answer file directly in the ISO (no network fetch, sidesteps decision record 16's guestfwd blocker entirely), and the prepared ISO's default boot entry became "Install Proxmox VE (Automated)" with a 5-second countdown — genuinely zero keystrokes. The installer's own literal completion line was captured directly for the first time via this path too: `Finished: 'ok' Installation finished - auto rebooting in 5 seconds ..`, exactly matching decision record 03's original finding. A genuine, unplanned discovery: the real installer's own generated `/etc/network/interfaces`, under this project's `[network] source = "from-dhcp"` answer combined with this host's IPv6-only SLIRP/SLAAC environment, is **structurally identical to Fixture A** — no hand-staging was needed, confirming Fixture A was never a synthetic test construct. Captured the file's MD5 and confirmed no IPv4 address/route on first boot, then issued a real guest `reboot` and re-checked: **MD5 unchanged, same no-IPv4 broken state reproduced exactly**. See [decision-records/22-diagnostic-tools-real-verified.md](decision-records/22-diagnostic-tools-real-verified.md) (the headless-install methodology) and [23-gateD-fresh-boot-reproduces-fixture.md](decision-records/23-gateD-fresh-boot-reproduces-fixture.md). One honest scope note: `drive_setup_install.py` boots legacy BIOS, not UEFI/OVMF — this pass demonstrates the reproduction property Gate D cares about via a real guest reboot, not fresh-OVMF-NVRAM firmware state specifically (that was Milestone 0 Investigation 4's separate, differently-scoped finding). Gate E (first boot waits for real local approval) and Gate F (five diagnostic tools persist after reboot) remain — Gate F is now cheap to close given decision record 22 already proved install+function for all five tools.

**Gate F passes (2026-09-23).** All 5 diagnostic tools installed on a real automated-install target (same headless `--fetch-from iso` methodology), confirmed `ii` via `dpkg -l` and `iperf3` confirmed `disabled`/`inactive`, then the guest was rebooted for real and every check re-run with an identical result: all 5 tools still installed, `iperf3` still `disabled`/`inactive`. See [decision-records/24-gateF-tools-persist-after-reboot.md](decision-records/24-gateF-tools-persist-after-reboot.md). **Gates A, B, C, D, and F now all pass with direct evidence.** Gate E (first boot waits for real local approval) is the one remaining gate — it depends on `firstboot_statemachine.py`/`setup_intent.py`, which still only exist in `experiments/m0-inv6/`/`experiments/m0-inv9/` and have not been promoted to `baseline/lib/`. That promotion is Milestone 1's next real gap.

**Gate E passes (2026-09-23). All six Milestone 1 Gates (A–F) now pass with direct evidence.** `setup_intent.py` promoted verbatim from decision record 09's prototype (13 new tests). A new, thin `baseline/lib/firstboot_statemachine.py` wraps the already-real, already-tested `firstboot_network_repair.py` flow with a durable completion marker/journal (fsync file + fsync directory + atomic rename, matching `setup_intent.record_consumption`'s proven pattern) — it does not reimplement discovery, the exact-diff proposal, or the CONFIRM-gate, all reused unchanged. 10 new tests, 196/196 suite passing. Real-verified against a fresh automated-install target with a genuinely broken (Fixture-A-shaped) network config: automatic discovery + exact-diff proposal shown → **44 seconds of zero input, confirmed content-identical** (byte-diff isolated to the blinking cursor, verified visually) → CONFIRM typed → a real fail-closed refusal was hit and honestly reported (missing `baseline-repair-rollback` dependency) → re-run after delivering it → **real success**: `vmbr0 gained a new inet dhcp stanza (existing inet6 stanza preserved); address=10.0.2.15 gateway=10.0.2.2`, independently confirmed via `ip -4 addr`/`ip -4 route` → completion marker verified to actually block re-trigger on a second run. See [decision-records/25-gateE-firstboot-statemachine-promoted.md](decision-records/25-gateE-firstboot-statemachine-promoted.md). Not re-verified in this pass: the real systemd-unit/tty1-takeover boot path (state machine was run from an interactive shell instead) and interrupted-run recovery — both flagged as real, stated gaps, not assumed proven.

**Gate E rebuilt as the real, integrated A→E→F path (2026-09-24) — the missing closeout step is now closed.** The prior Gate E pass proved the state-machine mechanics in isolation; `firstboot_statemachine.py` was substantially rewritten to actually connect Proxmox detection, Gate A's real repair pipeline, and package install/verification for the five diagnostic tools behind **one** combined tty1 screen and **one** CONFIRM gate, as a real nine-state durably-journaled machine (`created → detected → discovered → proposed → confirmed → network_repaired → packages_installed → packages_verified → committed`), gated so the completion marker is written only after package installation **and** functional verification both succeed. 19 new/rewritten tests explicitly covering interruption recovery, corrupted journals, declined authorization, network failure blocking package install, and package failure blocking completion — 205/205 suite passing.

Deployed via the real `boot/provision.sh` path (all referenced files cross-checked present *before* launching QEMU, per instruction) onto a fresh automated-install target, after finding and working around a real, honestly-flagged gap: `provision.sh` cannot fully complete when run interactively from the very tty1 session it disables partway through — completed the remaining `systemctl enable` calls from tty2 instead. One real reboot then produced the full integrated proof: the real combined tty1 screen (Proxmox detection + exact network diff + five packages + rollback/verify description + setup-intent integrity status), **46 seconds of zero input confirmed byte-for-byte identical** (stronger than decision record 25's cursor-blink-affected result), a real typed `CONFIRM`, real network repair success (independently verified via `ip`), real package install + verification (all five `dpkg -l` confirmed, `iperf3` disabled/inactive), all nine journal states confirmed present in order — and then, on the same boot, the real Baseline Textual TUI correctly took over tty1 (`BaselineOS — online - lifeline up`) while tty2 stayed a normal, unaffected login. A second real reboot proved full persistence: Baseline appears on tty1 immediately (no firstboot delay), tty2 still works, networking and all five packages persisted, `iperf3` still safe, first-boot did not rerun. See [decision-records/26-gateE-integrated-real-proof.md](decision-records/26-gateE-integrated-real-proof.md).

**One run has now demonstrated the integrated A→E→F path end to end — this is the closeout step both this record and decision record 25 flagged as the one thing separate Gate passes hadn't yet proven together.** Milestone 1 is not being declared complete on this basis alone: `settings_web.py`/GUI work remains explicitly out of this path by design (next milestone), `provision.sh`'s interactive-from-tty1 gap is unfixed (documented, not patched), and setup-intent's real signed-bundle use is still unwired (informational-only in this flow, as scoped).

**Milestone 1.1 provisioning closeout complete (2026-09-24) — the `provision.sh` release blocker is fixed and re-proven from two completely fresh images.** `provision.sh` no longer disables or disrupts its own tty/session: it now stages every file, runs atomic pre-activation verification (`verify_fail()`-gated checks over every staged file, entry point, and unit), and only `systemctl enable`s the three units (never `--now`, never touching `getty@tty1` directly) — activation is deferred entirely to the next boot via the units' own `Conflicts=`/`Before=` ordering. Re-proven end-to-end from a **completely fresh** disposable image with **zero manual completion steps**: automated install → unattended `provision.sh` (`PROVISION_RC=0` on an unbroken tty1 session, both verification `PASS` lines confirmed in `provision.log`) → reboot → tty1 automatically shows the full integrated proposal with no manual intervention → 42-second indefinite-wait proof (16 pixels differ, all cursor-blink, everything else byte-identical) → tty2 confirmed unaffected → real `CONFIRM` → network repair, all five packages, `iperf3` safety, and the durable completion marker all independently verified → second reboot proves full persistence and non-repetition. See [decision-records/27-milestone-1.1-provisioning-closeout.md](decision-records/27-milestone-1.1-provisioning-closeout.md).

A **separate** fresh disposable run added the one proof the earlier passes hadn't attempted: killing the first-boot process for real, after a durable journal write, after CONFIRM but before commitment (a watcher polling the journal killed the process the instant it recorded `network_repaired`). Recovery on the next boot was proven, not assumed: the journal was byte-identical before and after reboot (no re-diagnosis, no re-apply), the resumed run cost 45ms of CPU against the original run's 668ms, no completion marker was ever written, and `iperf3` — which nothing else in a stock Proxmox install would pull in — is conclusively confirmed **not installed**, proving package installation correctly never ran. The existing state-machine implementation and authority model were not changed in this pass (no test exposed a defect in them); only `provision.sh` was fixed.

**Closeout distinctions** (full detail in decision record 27): QEMU-proven in this pass — unattended, session-safe provisioning; automatic tty1 handoff; indefinite wait with no timeout/default; unaffected tty2; the full integrated repair→install→verify→commit pipeline with independent verification of every claim; non-repetition after a clean reboot; and real crash recovery after a real kill. Unit-test-only — resume-from-`proposed`, declined/EOF confirmation, no-safe-candidate refusal, corrupted-journal handling, and individual package/iperf3 failure modes (existing `test_firstboot_statemachine.py` coverage, unchanged). Physical-hardware-unverified — real sensors, NVMe/SMART on physical drives, physical NIC topology, Wi-Fi/tether recovery, GPU/USB, the intended external drive, preserving the current BaselineOS drive's state, and power-loss (vs. process-kill) recovery. Deferred — setup-intent enforcement remains informational/integrity-only; none of schema constraint, target/expiration/action-allowlist checks, replay protection, or gated consumption were implemented or implied.

With this closeout, Milestone 1's original goal — a reproducible, unattended path from a fresh Proxmox substrate back to the original Baseline functionality — is functionally complete in the disposable-QEMU reference environment. Physical-hardware deployment is the explicit next validation step, not a reason to discount this milestone. GUI/browser sandbox work, the Proxmox capability adapter, and any physical-drive work remain deferred to a future milestone and were not started in this pass.

Parent: [drive-setup-gui-v2-prd.md](drive-setup-gui-v2-prd.md) §9 (Milestone 1), built on [milestone-0-plan.md](milestone-0-plan.md)'s closed-out evidence (commit `519f367`, accepted).
Queued, not started: the five diagnostic-tool collectors (PRD §5.9a — capability detection, normalized output, `iperf3` kept out of the passive AI-harness context blob) are Milestone 2 scope, explicitly sequenced after this milestone's Gates A–F close. No code for them exists yet; do not begin implementation until Gate A passes.
Author: Claude Code, planning pass only — this document is the deliverable for this turn.

## Revision note (why this version exists)

The first version of this plan (commit `771b4c4`) was reviewed and **not approved**. The core objection: it treated destination networking as a "zero-cost discovered-fact check" folded into a Phase 7 end-to-end proof — but networking is the only genuinely unresolved uncertainty in the core image pipeline (Milestone 0 only ever observed IPv6-only behavior inside QEMU/SLIRP and explicitly scoped that finding to the test environment, never resolved it). Discovering a networking failure at the end of a nine-module build would invalidate work built on top of an untested assumption. This version moves that investigation to **Phase 0, before any production module is written**, and cuts everything else down to what one usable vertical slice actually needs — no storage ancestry, no MD RAID/LUKS, no generalized key management, no single expensive end-of-pipeline test standing in for incremental verification. Four cohesive module boundaries instead of nine, and six named gates instead of one big proof at the end.

## 0. Canonical repository — unchanged from v1

**`cliffthelin/BaseLineProx` (the `origin` remote) remains the sole canonical repository**, per `docs/SESSION_HANDOFF.md`'s explicit documentation (see v1's §0 for the full evidence, unchanged by this revision). This plan pushes to `origin` only. No push to `cliff` (`cliffthelin/baseline`) happens unless the user explicitly says otherwise.

## 1. Hard constraints carried forward from Milestone 0 (unchanged)

- No physical block-device writes of any kind.
- No physical-device argument, dormant flag, helper action, or polkit capability accepted or created anywhere in Milestone 1 code — enforced structurally, not by convention.
- All experimentation stays under `experiments/`; promoted code moves into `baseline/lib/`, `packaging/baseline-drive-setup/`, and `tests/unit/drive_setup_tests/` only with real test coverage.
- No host system modification: no `apt install` to the development host, no APT source changes, no systemd unit installed on the development host.
- Setup-intent signing uses a **single synthetic Ed25519 test key per run, generated fresh each time, discarded after** — no key management subsystem, no rotation, no revocation list, no persistence of any key across runs. Production trust-anchor provisioning remains explicitly out of scope (§7).

## 2. Scope — one demonstrable vertical slice, not a platform

Per review, Milestone 1 is now exactly this chain, and nothing beyond it:

1. Verified source artifacts (ISO + assistant package, via the existing GPG/hash chain already proven in Investigation 1/2 — reused, not rebuilt).
2. Safe ephemeral credential generation (argv-free, per Investigation 2/3).
3. Pinned, single-use answer delivery (per Investigation 2/3's accepted design).
4. Guarded ISO preparation and cleanup (never trusting exit codes, per Investigation 2's confirmed finding).
5. Sparse-image-only installation.
6. Explicit installer-success verification (the literal `Finished: 'ok'` signal, per Investigation 3's own corrected mistake — never inferred).
7. Disk-first reboot and fresh-OVMF verification (`once=d` mandatory, per Investigation 4's confirmed defect).
8. **Resolved destination networking** — new in this revision, and the actual gating concern; see Phase 0.
9. tty1 discovery and indefinite local authorization (the corrected state machine from Investigation 6).
10. Authorized installation of the five diagnostic packages, confirmed to persist after reboot.
11. A machine-readable manifest and a concise evidence report.

Everything not in this list is removed or deferred — see §6.

## 3. Phase 0: destination-network resolution (runs first, before any production module is written)

**Why this comes first**: every later phase either runs inside a QEMU guest reachable by the answer server (steps 2–7) or depends on the *installed* system being reachable for anything beyond a local console session (step 9's tty1 flow doesn't strictly need network, but the diagnostic-package install and any real Milestone 2 work eventually will). Milestone 0 observed IPv6-only `fec0::/64` addressing throughout every QEMU session but never investigated *why*, and explicitly scoped the finding to the test environment rather than resolving it. That gap is closed here, first, cheaply, using only the disposable sparse-image/QEMU environment and existing non-interactive permissions — no new privilege, no host change.

**Procedure**:

1. **Reproduce once.** Regenerate one fresh sparse-image install using the exact proven pipeline from Investigation 3 (`run_final.sh`, unmodified) and confirm the `fec0::/64`-only result reproduces, on this specific run, before investigating further — not assumed still true from a prior investigation's evidence.
2. **Capture, don't infer, every layer of evidence**:
   - The answer file's actual `[network]` section as generated (`source = "from-dhcp"` per Investigation 1's fixture — confirm this is still what Milestone 1's credential/answer modules would actually produce).
   - The installer-generated network configuration on the installed disk (`/etc/network/interfaces` or equivalent, inspected via a disposable QEMU guest boot with the target disk attached — never by mounting the sparse file directly on the host, consistent with every Milestone 0 investigation's "no loop device against anything but a controlled fixture" discipline; here there is no fixture, so no loop device at all — boot and inspect from inside the guest).
   - The kernel command line the installed system actually boots with (`/proc/cmdline` inside the guest).
   - DHCP lease state, interface state (`ip addr`, `ip link`), routes (`ip route`, `ip -6 route`), and DNS configuration (`/etc/resolv.conf` or `resolvectl status`) — captured from inside the running guest, not assumed from configuration files alone.
   - Which networking implementation is actually active and enabled — `systemctl status systemd-networkd`, `systemctl status NetworkManager`, presence/absence of `ifupdown`/`ifupdown2`, checked directly rather than presumed. This directly answers the review's correction: the test identifies what's actually running, it does not presume NetworkManager (or any other specific implementation) is the relevant one.
3. **Determine the source, by elimination against direct evidence, not by guessing**: does the IPv6-only behavior originate in (a) the answer file's own `from-dhcp` request never asking for IPv4 explicitly, (b) `proxmox-auto-install-assistant`'s installer environment itself, (c) QEMU SLIRP's DHCP server behavior specifically (SLIRP is known to serve both v4 and v6, so this needs its own direct check — e.g. does a `tcpdump`/`dhclient`-visible DHCPv4 offer even reach the guest), (d) whichever networking implementation §3.2's last check identified as active, or (e) some first-boot transformation applied after install. Each of these produces a distinguishable evidence signature; the goal is to land on one, evidenced answer, not a plausible-sounding guess.
4. **Test only the smallest plausible correction** implied by step 3's finding — e.g. if SLIRP itself is the limiting factor, confirm whether a plain `-netdev user` without additional flags actually offers IPv4 in a trivial standalone QEMU boot (unrelated to the installer) as a control case; if it's the installed system's own active networking implementation, test the smallest configuration change that implementation would need (e.g. one interface-config line), not a rewrite of the networking stack. No speculative multi-option matrix — one hypothesis, one smallest test, evaluated on direct evidence.
5. **Prove usable networking after a fresh boot**, with every one of these five properties independently confirmed, not inferred from any subset:
   - An assigned address (v4 or intentionally-chosen v6 — see below).
   - A correct default route.
   - DNS resolution (an actual resolved lookup, not just `resolv.conf` contents).
   - Outbound HTTPS reachability (an actual successful TLS connection to something, not just a route existing).
   - **Persistence across reboot** — the same five checks repeated after a clean reboot of the same installed disk, not just the first boot.
6. **If networking cannot be corrected safely within this phase's scope, define a fail-closed detection and stop condition, and do not build the remaining pipeline around broken networking.** Concretely: if step 4's smallest correction doesn't produce a passing step 5, Phase 0 ends with an explicit, evidence-backed `networking_unresolved` result, a precise description of what was tried and what remains broken, and **Gate A does not pass**. **`networking_unresolved` is a blocking outcome, not an alternative completion criterion** — it is a valid and legitimate investigation result (matching Investigation 8's precedent that "no sufficiently strong mechanism found" is a valid, honest conclusion, not a failure to hide), but it does not satisfy Gate A under any framing. If Phase 0 ends this way, **no later-phase module gets written** and the decision to either (a) accept IPv6-only operation as intentional (if that's genuinely sufficient for Baseline's actual needs — a real question for the user, not this investigation to decide unilaterally) or (b) escalate with real diagnostic capability (which may require authorization this phase is instructed to avoid) is handed back explicitly rather than absorbed silently or treated as "close enough" to proceed on.

**Authorization discipline for this phase specifically**: every step above uses only a disposable sparse image, QEMU, and already-confirmed non-interactive permissions. If any specific diagnostic step would require sudo, pkexec, polkit, a keyring, or any interactive prompt, that specific step is skipped and recorded as unavailable evidence — Phase 0 continues with whatever it can determine from the rest, and reports the gap plainly rather than working around it.

**Outcome of the first Phase 0 run**: `networking_unresolved`, under this section's original v2 wording — see [decision-records/10-m1-phase0-network-resolution.md](decision-records/10-m1-phase0-network-resolution.md). That evidence (a genuinely broken persisted static config, in more than one address-family shape) stands unchanged. What changed is §3a below: rather than treating this as an installer-pipeline blocker requiring a further installer-side correction, it's now understood as exactly the class of fault an already-built, already-tested Baseline capability exists to repair post-install, at first boot, under real local authorization.

## 3a. Architecture revision: broken destination networking is a first-boot discovery input, not an installer failure

Discovered during Milestone 1 planning (not before): `cliffthelin/baseline` branch `repair/host-network-reset-to-dhcp` (commits `0368d7a`/`8509af6`, draft PR #1, 34/34 unit tests passing) already implements a thorough, transactional, rollback-protected, tty1-authorized action — `reset_interface_to_dhcp()` — for exactly this fault class: a management interface (physical, bridged, bonded, or VLAN) stuck on a broken static IPv4 configuration. Building new repair logic in Milestone 1 would have duplicated real, reviewed, tested work. See [decision-records/11-repair-branch-comparison.md](decision-records/11-repair-branch-comparison.md) for the full comparison this revision is based on — nothing has been copied or merged; this section only changes the *architecture* Milestone 1 targets.

**Revised model**:

- Installer-stage networking (the answer-fetch HTTPS path, §3's guest-fwd-isolated answer server) is used **only** to retrieve the answer and complete installation — it was never meant to prove the *installed* system's networking works, and Milestone 1 stops asking it to.
- An incorrect or non-functional persisted destination network configuration (Phase 0's actual finding) is **an input to first-boot fact discovery**, exactly like any other discovered fact (NIC names, link state, storage) — not, by itself, a failed installation. `network.py::check_lifeline()` already exists and already produces exactly the `gateway_reachable`/`address_assigned` failure facts this case needs.
- First boot's tty1 state machine displays the detected topology (via `topology.py::derive_target()`) and the proposed repair (convert the derived target interface to DHCP) — the same "propose, then wait" shape already proven in Investigation 6, with `repair.py`'s pipeline standing in as the "apply" action instead of the placeholder executor Investigation 6 used for firewall policy.
- It waits **indefinitely** for genuine local authorization — no change to the CONFIRM-gate discipline already proven and re-verified in Investigation 6; `reset_interface_to_dhcp()`'s own `operator_present` parameter is designed for exactly this ("set only by the TUI's own local confirmation flow, never settable remotely").
- After approval, the existing bounded repair action executes: independent rollback armed before any write, atomic stanza rewrite, `ifreload -a`, target-bound verification (not global `check_lifeline()`, which a healthy USB tether or second NIC could satisfy while the actual target stays broken).
- Only after target-bound connectivity succeeds may package installation (the five diagnostic tools) and any later setup continue — this is a straightforward re-ordering of Milestone 1's existing sequence (§2 items 9–10), not a new mechanism.
- A failed repair, or an ambiguous topology (`topology_ambiguous`, `no_static_config` — the exact refusal Fixture A's IPv6-only case triggers, correctly, per decision record 11 §2/3), rolls back or refuses and **remains at tty1** — no automatic retry, no silent fallback, no proceeding to package installation on unresolved networking.

**What this does not change**: Phase 0's underlying evidence (decision record 10) is not overturned — the persisted static config genuinely doesn't work, in either address-family shape tested. What changes is that this is no longer framed as something the *installer pipeline* must fix before Milestone 1 can proceed; it's the first real test case for a repair capability that already exists and was already designed for exactly this.

## 4. Module boundaries — four cohesive boundaries, not nine

| Boundary | File(s) | Responsibility | Promoted from |
|---|---|---|---|
| **1. Artifact acquisition and verification** | `baseline/lib/drive_setup_acquire.py` | Reproduces Investigation 1/2's exact GPG-signed-`Release` → hash-pinned-`Packages` → hash-pinned-`.deb`/ISO chain against cached fixture metadata; extracts the assistant binary without touching the host package database. | Investigation 1/2's verification commands, not previously written as reusable code — this is the one genuinely new module in this boundary. |
| **2. Answer delivery and ISO preparation** | `baseline/lib/drive_setup_answer.py` | One cohesive module covering: argv-free one-time credential generation and hashing; the ephemeral single-use, TTL-bounded, hardware-fact-checked HTTPS answer server; the postcondition-based wrapper around `prepare-iso` that never trusts exit codes and owns staging-directory cleanup on every path; and workspace/path validation ensuring every target resolves to a controlled regular file. These four concerns are tightly coupled in practice (the credential feeds the answer content, the wrapper/workspace guard the call that consumes it) and are combined into one boundary rather than four separate files. | `experiments/m0-inv3/credential.py`, `answer_server.py`, `wrapper.py`, `workspace.py` — consolidated. |
| **3. Image installation and boot verification** | `baseline/lib/drive_setup_install.py` | QEMU invocation construction (sparse-file-only, `guestfwd`-isolated networking, mandatory `-boot order=c,once=d`), screendump-based explicit-success detection, legacy-BIOS and fresh-OVMF-NVRAM boot verification. **No longer responsible for producing working destination networking** — that's boundary 4's job now, per §3a. | New orchestration, reusing QEMU-invocation and screendump patterns from `experiments/m0-inv3/run_e2e_install.py` and Investigation 4's commands. |
| **4. First-boot setup, network repair, and evidence** | `baseline/lib/firstboot_statemachine.py` (promoted, unchanged from its corrected version) + `baseline/lib/setup_intent.py` (promoted, minimal — see §7) + **`baseline/lib/repair.py`, `topology.py`, `ifnet_config.py`, `repair_rollback.py` (integrated, not rebuilt — see §3a and decision record 11)** | The proven propose → indefinite-`CONFIRM` → apply → verify → commit state machine and tty1 ownership; minimal setup-intent canonical-format signing/verification with a synthetic per-run key; **the existing host-network repair pipeline wired in as the state machine's networking-repair action, gated the same CONFIRM way as any other consequential action**; the diagnostic-package install step gated behind both confirmation *and* successful target-bound network verification; the manifest (§8) and evidence-report generation. | `experiments/m0-inv6/firstboot_statemachine.py` (corrected version only) + `experiments/m0-inv9/setup_intent.py` (near-verbatim, already minimal) + `cliffthelin/baseline` branch `repair/host-network-reset-to-dhcp` (integration only — see §3a; nothing copied yet, this is the target for when integration work is authorized). |

Two-plus files under one boundary (#4) rather than one, because the state machine, the setup-intent verifier, and now the network-repair pipeline are all "first-boot trust, confirmation, and bounded-action execution" concerns that were already separately proven and don't benefit from forced merging — the boundary is conceptual (owned by one area of responsibility), not a literal one-file-per-boundary rule.

**Explicitly not promoted or built in Milestone 1**: `storage_ancestry.py`, any MD RAID/LUKS fixture code, any exclusive-access/`O_EXCL`/automount-handling code, any production key-provisioning/rotation code, `pinned_client.py`/`preflight.py` (kept as test-only infrastructure, not shipped), `vnc_type.py` (test-harness only). See §6 for the reasoning behind each.

## 5. Gate sequence

Each gate is a stop/go checkpoint with its own evidence. **A failed gate stops progress into later phases and produces a written evidence note — it does not trigger a compensating framework, a fallback design, or scope expansion to "solve" the failure at the same time it's discovered.** That decision goes back to the user, the same way Investigation 8's genuinely-unresolved outcome was reported plainly rather than forced into a false "solved."

| Gate | Condition | Tied to |
|---|---|---|
| **A** (redefined, v3) | **On a fresh installed image carrying the reproduced broken static configuration (either fixture shape from decision record 10), the existing Baseline repair path — after explicit tty1 authorization — establishes usable destination networking, and address/route/DNS/HTTPS are verified through the selected interface, persisting across reboot.** | Phase 0 (fault reproduction, decision record 10) + decision record 11 (comparison) + Boundary 4 (repair integration) — **not yet attempted under this definition. The v2 wording of Gate A (networking must already work right after install, with no repair step) is retired — see §3a.** |
| **B** | Prepared ISO verified without booting — `inspect-iso` output and a raw byte scan confirm fetch mode, URL, cert fingerprint, and the absence of forbidden strings (plaintext password, hash, full answer content in HTTP mode), matching Investigation 2/3's postcondition set. | Boundary 1 + 2 |
| **C** | Installation reaches explicit success — the literal `Finished: 'ok'`/`Installation done` sequence captured via screendump, never inferred from QEMU exit code or partition structure alone. | Boundary 3 (install half) |
| **D** (revised meaning) | Fresh-OVMF boot reliably reproduces the same persisted (broken) static configuration Phase 0 observed, confirming the installed image is a stable, correct fixture for Gate A's repair test — **not** that networking already works at this point, which is no longer expected before repair runs. | Boundary 3 (boot half) |
| **E** | First boot waits for real local approval — the 40-second-idle-no-change plus post-`CONFIRM` apply/verify/commit evidence, reproduced with the same discipline Investigation 6 used after its own correction. **Now also covers authorizing the network-repair action specifically**, per §3a. | Boundary 4 (state machine + repair integration) |
| **F** | Packages persist after reboot — the five diagnostic tools installed only after Gate E's confirmation **and** Gate A's target-bound connectivity verification succeeds, `iperf3` confirmed disabled/inactive immediately after install **and again after a full reboot**, matching Investigation 5's already-proven check. | Boundary 4 (package step) |

Gates run in order; B–F do not start implementation until A passes, per the review's core instruction — this is unchanged by the redefinition, only what A actually tests has changed. Within B–F, each gate's own module can be developed and unit-tested incrementally (TDD, per this project's standing rule) — the gates are integration checkpoints, not a ban on incremental work within a boundary.

## 6. Removed or deferred scope, and why

| Item | Disposition | Reasoning |
|---|---|---|
| MD RAID / LUKS-on-LVM validation | **Deferred, likely to Milestone 3 timing, not Milestone 1** | Not needed for an image-only installer that never resolves live-host storage ancestry against anything but the disposable image it itself created — this concern only exists once physical-device selection (excluding a currently-mounted host device's ancestry) is in scope, which is Milestone 3, not Milestone 1. |
| Storage-ancestry productionization (`storage_ancestry.py`) | **Deferred entirely, not part of Milestone 1** | Same reasoning — physical-device support is structurally absent through Milestone 1, so there is no live host device whose ancestry ever needs excluding. Investigation 7's module and its real-tool-validation gap both move to whichever milestone actually needs live-ancestry exclusion (Milestone 3). |
| Physical-device exclusivity (`O_EXCL`, `flock`, monitoring) | **Deferred to Milestone 3, unchanged from Investigation 8's own conclusion** | Investigation 8 already deferred this; nothing in Milestone 1's image-only scope requires it. |
| Automount/disconnect handling | **Deferred to Milestone 3** | Same reasoning — only relevant once a real removable/physical device exists in scope. |
| Production key provisioning or rotation | **Deferred, explicitly out of scope for Milestone 1** | See §7 — Milestone 1 uses one synthetic per-run key, never a managed key lifecycle. |
| Any module required only for physical-drive support | **Deferred to Milestone 3** | Consistent with §5.4's structural-absence requirement — nothing in this list gets built even partially. |
| A single expensive end-to-end proof standing in for incremental verification (v1's Phase 7) | **Replaced by the six-gate sequence in §5** | The review's point: one big proof at the end creates a second "learn too late" failure mode structurally identical to the networking one this revision exists to fix. Gates B–F each produce their own evidence as work proceeds, not one terminal checkpoint. |

## 7. Setup-intent: minimal, not a subsystem

Per review, Milestone 1's setup-intent handling stays deliberately small:

- **Canonical format**: unchanged from Investigation 9's promoted `setup_intent.py` — sorted-key, compact-separator JSON with strict duplicate-key rejection at parse time.
- **Synthetic signing key**: exactly one Ed25519 keypair generated fresh per build/test run, held in memory or a gitignored temp path, discarded at the end of the run. No key ever persists across runs, no key is ever committed, no key ever stands in for a production trust anchor.
- **Strict verification**: the same fail-closed `verify_intent()` chain proven with 16/16 synthetic tests in Investigation 9 — schema allowlist, expiry, target-match, action-set membership, existence-based replay ledger — reused essentially unchanged.
- **Local authorization**: verification success is a precondition for *proposing* the confirmed action on tty1 (Gate E), never itself an execution trigger — unchanged from Investigation 9's own stated principle.
- **Honest integrity-only labeling**: Milestone 1's bundle verification key is stored beside the bundle (no independent provisioning channel exists yet), so it is documented, in the manifest and evidence report, as an **integrity/corruption check**, not authentication — exactly the distinction Investigation 9 established. No language in Milestone 1's code, tests, or evidence report claims more than this.
- **What is explicitly not built**: key rotation, multiple trusted keys, a revocation list, remote/out-of-band key distribution, or any persistence layer for keys — a "key-management subsystem" in miniature is exactly what this section exists to prevent, per the review's direct instruction.

## 8. Manifest and evidence report (unchanged in spirit from v1, reduced in content)

Machine-readable JSON manifest per completed run, network fields promoted from Phase 0's afterthought status to first-class:

```json
{
  "schema": "baseline.m1-build-manifest.v1",
  "run_id": "<uuid>",
  "started_at": "<ISO8601>",
  "completed_at": "<ISO8601>",
  "source_iso": {"sha256": "...", "verified_via": "gpg-signed-release-chain"},
  "gate_results": {
    "A_networking_proven": "pass | fail | networking_unresolved",
    "B_iso_verified": "pass | fail",
    "C_install_success": "pass | fail | indeterminate",
    "D_fresh_boot_networking": "pass | fail",
    "E_firstboot_confirmation_gate": "pass | fail",
    "F_packages_persist_after_reboot": "pass | fail"
  },
  "networking": {"addressing": "ipv4 | ipv6 | dual", "source_of_behavior": "answer-file | installer | slirp | <networking-impl> | first-boot", "correction_applied": "none | <description>"},
  "setup_intent": {"key_type": "synthetic-per-run", "label": "integrity-only"},
  "artifact_retention": {"credentials_purged": true, "temp_isos_deleted": true, "test_key_discarded": true}
}
```

The evidence report is the same Markdown-narrative counterpart as v1, but organized by gate (A–F) rather than by phase, each gate's section carrying its own pass/fail and linked evidence — this directly supports "a failed gate stops later phases and produces evidence" rather than requiring a full run to see any result.

## 9. Artifact retention rules (unchanged from v1)

| Category | Retention |
|---|---|
| Ephemeral one-time install credentials (plaintext) | Never written to disk — process memory only. |
| Password hash | Exists only inside the answer file / prepared ISO / installed disk's own shadow file — not separately retained. |
| Answer files | Deleted immediately after the one QEMU session that consumes them. |
| Prepared ISOs | Deleted after the one install that used them — never reused. |
| Synthetic setup-intent key | Discarded at the end of each run — never checked in, never reused. |
| Disposable sparse install images | Deleted after their evidence is captured, unless explicitly reused within the same gate sequence (e.g. the same image serving Gates C and D). |
| Build manifest + evidence report | Retained — no secret fields by construction. |
| Phase 0's own investigation artifacts | Deleted after Phase 0's findings are recorded, same discipline as every Milestone 0 investigation. |

## 10. Dependency and privilege boundaries (unchanged from v1, trimmed)

- No new host packages — the extracted assistant binary plus already-present `xorriso`, `qemu-system-x86_64`, `ovmf` are sufficient.
- No new Python dependencies beyond `cryptography` (setup-intent) and stdlib.
- No privileged operation on the development host — every QEMU session runs unprivileged against workspace-local files; `pkexec` is never invoked, since the physical-device capability remains structurally absent.
- Setup-intent keys are synthetic, per-run, and discarded (§7) — no keyring, credential manager, or system secret store is touched.

## 11. Criteria for declaring Milestone 1 complete (revised, reduced)

1. Gate A, under its redefined (v3) wording, passes: the existing repair path establishes and verifies usable destination networking after tty1 authorization, on the real reproduced-broken fixture, persisting across reboot — **before** boundary 3/4's networking-dependent pieces are considered done. If the redefined Gate A also fails, an explicit result is recorded and handed back to the user rather than built around, per the same discipline as v2's `networking_unresolved` handling. **Current status: Gate A has not yet been attempted under its redefined wording — decision record 10's failure was against the retired v2 definition (networking must work with no repair step) and decision record 11 is the read-only comparison this redefinition is based on. No §4 module has been started; the integration described in §3a is not yet authorized or implemented.**
2. Gates B through F each pass with their own recorded evidence, in order.
3. All four module boundaries in §4 exist under `baseline/lib/` with tests written first (TDD) and passing. **Coverage is a floor, not proof**: ≥80% line coverage is a minimum sanity check, but a module is considered actually verified by its behavioral tests and its gate's own evidence (§5), not by the coverage percentage alone — a well-covered module that hasn't passed its gate's real evidence check is not "done," and a module with thin coverage but strong, specific behavioral tests targeting its fail-closed conditions (§4 of v1's invariants, carried forward conceptually) is the higher bar to meet, not the percentage.
4. The manifest (§8) and evidence report have been generated from at least one real run and reviewed.
5. §9's retention rules are verified on at least one full run — no leftover credential-bearing artifact.
6. Setup-intent's manifest/evidence-report language states "integrity-only" plainly, per §7 — no overclaim.
7. Nothing has been pushed to `cliffthelin/baseline`.
8. A short closeout note (matching this session's decision-record honesty standard) states what passed, what didn't, and what's explicitly carried into Milestone 2 or Milestone 3 — including, if applicable, an unresolved networking result, which would itself be a valid and important Milestone 1 finding rather than a failure to hide.

---

This revised plan is the deliverable for this turn. No code has been written, no privileged interface invoked, no package installed, and no authorization request triggered while preparing it. Awaiting review before any implementation phase — including Phase 0 — begins.
