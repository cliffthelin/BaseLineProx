# chat - block 001 (open)

harness.py, providers.py, the Chat tab. Rotate to 002.md once this file
exceeds ~400 lines or ~15 entries - see INDEX.md.

---

## 2026-09-20 ~13:xx - Chat-tab quality assessment (decision only, no code yet)

**Requested via:** chat ("What gaps are there that would prevent me
  from having full quality Claude Code conversation / development from
  the Chat tab", then "Yes, then let's return to Claude if you feel we
  are ready...")
**Context:** `harness.ask()` calls `claude -p <prompt> --tools ""` -
  a single stateless call per message, zero tools, only the
  hardware/network JSON blob as context. User wants a real conversation
  with visible/toggleable access scope, and asked me to research
  before implementing.
**Considered:**
  1. Embedding the actual interactive `claude` CLI in a raw PTY inside
     a Textual widget (what the user's original phrasing implied - a
     literal "tab out of Claude Code back to Baseline" command). Real
     effort: a terminal-emulator-inside-a-terminal-emulator, plus
     needing to explicitly carve out Baseline's own bindings from
     whatever has PTY focus. No such widget is currently available in
     this project; not attempted.
  2. Keeping Baseline as the sole terminal/renderer, but making
     `harness.ask()` stateful and richer via CLI flags confirmed
     present in the installed `claude --help`: `--session-id
     <uuid>`/`-c/--continue` for real multi-turn memory, `-p
     --output-format stream-json` for real incremental streaming,
     `--allowedTools`/`--disallowedTools` for a real, named, auditable
     access-scope control (not all-or-nothing).
  Chose (2): it delivers everything asked for functionally (memory,
  streaming, visible scope) without the PTY-embedding risk, and
  because nothing ever captures raw keyboard focus away from Baseline,
  the "escape hatch back to Baseline" problem the user was solving for
  doesn't exist in this design - Tab/q/r keep working everywhere,
  always.
**Requirement determined:** Not yet finalized as a concrete acceptance
  test - this was a scoping/architecture conversation. Proposed shape:
  session-id-per-chat-session + `-p --output-format stream-json` +
  `--allowedTools` exposed as a header toggle showing the live-granted
  scope, plus a condensed one-line header (title | tabs | scope |
  memory | copy | datetime).
**Files changed:** none yet.
**Verification:** N/A - no code changed.
**Pointer update:** chat row added to INDEX.md, marked "plan agreed,
  not yet implemented."
**Docs update:** none yet - this changelog entry *is* the record for
  now; promote to a proper design doc if/when implementation starts.

---

## 2026-09-26 ~xx:xx - Warm per-process session implemented; write-ability scope decided separately

**Requested via:** chat (a cross-session design review flagged
  reachability ["always available"] and execution autonomy ["push
  fixes without asking"] as two separate axes being conflated; asked
  to act on both, with an explicit constraint on the second: any write
  ability must be asked-for, scoped, and session-only, never a
  standing grant)
**Context:** This entry's own 2026-09-20 record above had scoped but
  not implemented `--session-id`/`-c` session continuity for
  `harness.ask()`. Separately, `docs/salvage/OLD_BASELINE_NOTES.md`'s
  rejection of the old project's "Agentic (YOLO)" execution mode
  needed either re-affirming or an explicit, written reversal - it was
  at risk of silent drift in either direction otherwise.
**Considered:** For the session work, no new options beyond what
  2026-09-20 already chose - implemented that plan directly. For the
  write-ability question, considered leaving the read-only narrowing
  untouched (rejected: user explicitly wants write ability to exist)
  versus a full reversal to unattended execution (rejected: exactly
  the OpenClaw/"Agentic YOLO" failure shape already rejected once)
  versus a scoped, ask-first, session-only grant modeled on
  `testpersistence/grants.py`'s existing `Grant` shape (chosen -
  narrower than what was rejected, reuses a pattern already proven in
  this codebase).
**Requirement determined:**
  - Session continuity: `build_ask_argv` returns `--session-id <uuid>`
    on a session's first call and `-c` on every later call in the same
    process; `--tools ""` unchanged on both. Proven via unit tests
    against an injected fake runner, not a real CLI call.
  - Write-ability grant: decided in writing (decision record 32), not
    implemented. No code changes this session grant the harness write
    or execution ability of any kind.
**Files changed:**
  - `baseline/lib/harness.py` - added `Runner`/`RealRunner`,
    `HarnessSession`, `build_ask_argv`; `ask()` now takes optional
    `runner`/`session` keyword args, defaulting to a real subprocess
    call and one warm module-level session.
  - `tests/unit/test_harness.py` - new, 8 tests (RED confirmed first:
    `AttributeError: module 'harness' has no attribute 'Runner'`
    before implementation).
  - `docs/design/decision-records/31-chat-tab-warm-session.md` - new.
  - `docs/design/decision-records/32-harness-write-scope-grant.md` -
    new; decision only, no code.
**Verification:** RED confirmed before implementation; 8/8 new tests
  passing; full suite 618/618 (610 before this change), no
  regressions; `harness.py` byte-compiles clean. Not yet verified: a
  real `claude` CLI round trip proving `--session-id`/`-c` actually
  preserves memory on real hardware.
**Pointer update:** chat row's entry count/date/summary updated below.
**Docs update:** decision records 31 and 32 added under
  `docs/design/decision-records/`.
